<div class="w-full">
  <div class="text-center">
    <h1 class="quote">
       "Books are a uniquely portable magic." - Stephen King
    </h1>
  </div>

  <div id="books" class="grid min-w-full grid-cols-5 gap-7">
      <%= render partial: "books/book_list", collection: @books, as: :book %>
  </div>
</div>
